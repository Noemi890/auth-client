import React from "react"
import { Input } from "./Input"

export const Form = ({ user, set }) => {
  
  const handleSubmit = () => {}

  return (
    <Form>
      handleSubmit={handleSubmit}
      <Input />
      <Input />
    </Form>
  )
}